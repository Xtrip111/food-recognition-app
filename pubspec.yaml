name: food_recognition_app
description: Приложение для распознавания ингредиентов и получения рецептов.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.0
  path_provider: ^2.0.11
  path: ^1.8.2
  http: ^1.1.0
  cupertino_icons: ^1.0.5
  image_picker: ^0.8.7
  # google_generative_ai: ^0.1.0  # Удалено

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
